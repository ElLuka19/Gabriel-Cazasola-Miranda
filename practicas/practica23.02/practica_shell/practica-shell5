#!/bin/bash
# practica5.sh - Simulación de acceso restringido

echo "Mensaje confidencial" > secreto.txt
chmod 600 secreto.txt

# Intentar acceder con otro usuario (simulación, ejecutar manualmente con otro usuario)
chmod 644 secreto.txt
ls -l secreto.txt
chmod +x practica5.sh